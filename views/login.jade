extends layout

block content
 h1 Login

 form(method='post', action='/users/login')
  .addpostbox
   label.addpost Username
   input.addpost(name='username', type='text', placeholder='Enter username')

  .addpostbox
   label.addpost Password
   input.addpost(name='password', type='password', placeholder='Enter password')
  input.save(type='submit', name='submit', value='Login')
  br
  br
  hr
