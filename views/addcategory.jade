extends layout

block content
	h1=title

	ul.errors
		if errors
			each error, i in errors
				li.alert.alert-danger #{error.msg}

	form(method='post', action='/categories/add')

		input.addpost-input(name='name', type='text')
		input.save(name='submit',type='submit',value='Add')
